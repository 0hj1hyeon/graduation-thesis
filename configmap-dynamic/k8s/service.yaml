apiVersion: v1
kind: Service
metadata:
  name: configmap-dynamic-app-service
spec:
  type: NodePort
  selector:
    app: configmap-dynamic-app
  ports:
    - port: 80
      targetPort: 8082
      nodePort: 32082
